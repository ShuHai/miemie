var miemie = (function () {
'use strict';

var Array = function Array () {};

Array.prototype.hello = function hello (message) {
  var a = message || 'hello world';
  console.log(a);
};

// 数组去重
Array.prototype.unique = function unique (arr) {
  var result = [];
  arr.map(function (item) {
    result.indexOf(item) === -1 ? result.push(item) : null;
  });
  return result
};

var Mie = function Mie () {
  this.utils = new Array();
};

var miemie = new Mie();

return miemie;

}());
